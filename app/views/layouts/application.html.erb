<!DOCTYPE html>
<html>
  <head>
    <title>TypingApp</title>
    <%= csrf_meta_tags %>
    <%= stylesheet_link_tag    'application', media: 'all' %>
    <link href="https://fonts.googleapis.com/css?family=Inconsolata" rel="stylesheet">
    <%= javascript_include_tag 'application' %>
  </head>
  <body>
    <div class="container-fluid">
      <div id="type-console">
        <%= render 'partials/main' %>

          <div v-show="finishExercise" v-cloak>
            <div class="alert alert-success text-center" id="section-finished" role="alert">
              <br>
              <i class="fa fa-thumbs-o-up" aria-hidden="true"></i>
              <br>
              <br>
              <strong>Awesome!</strong> You are finished with {{ pageParentName }}.
              <br>
              <p>Please choose another Book or Open Source Project.</p>
              <br>
              <br>
            </div>
          </div>
      </div>

      <%= yield %>

    </div>
  </body>
</html>
