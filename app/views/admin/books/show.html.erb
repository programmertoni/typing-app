<div class="row">
  <div class="col">
    <h3>Book: <%= @book.name %></h3>  
  </div>
</div>

<div class="row">
  <div class="col">
    <table class="table">
      <thead>
        <tr>
          <th>Page number</th>
          <th></th>
          <th></th>
          <th></th>
        </tr>
      </thead>
      <tbody>
        <% @book.book_pages.each do |book_page, indx| %>
          <tr>
            <td><%= link_to "page #{book_page.number}", admin_book_book_pages_path(@book, book_page), class: "btn btn-secondary" %></td>
            <td><%= link_to 'Edit', admin_book_book_pages_path(@book, book_page), class: "btn btn-success" %></td>
            <td><%= link_to 'Delete', admin_book_book_pages_path(@book, book_page), method: :delete, data: {confirm: "Are you sure?"}, class: "btn btn-danger" %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>


<div class="row">
  <div class="col">
    <%= link_to "Add new Page", {controller: "book_pages", action: "new", book_id: @book.id} %>
    <%# <%= form_for [@book, @book_page], url: {action: "create"}, html: {class: "nifty_form"} do |f| %>
    <%#   <%= f.text_field :number, autofocus: true %>
    <%#   <%= f.submit "Add new Page" %>
    <%# <% end %>
  </div>
</div>
